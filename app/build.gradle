plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("com.chaquo.python")
}

android {
    namespace = "com.khan.pybox"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.khan.pybox"
        minSdk = 24
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"

        ndk {
            // arm64-v8a only: that's what the S22 Ultra (and effectively every
            // real phone from the last several years) runs. Building llama.cpp
            // for more ABIs just multiplies CI build time for binaries you'll
            // never install. Add "x86_64" back here if you ever need an
            // emulator build.
            abiFilters += listOf("arm64-v8a")
        }

        externalNativeBuild {
            cmake {
                arguments += listOf("-DANDROID_STL=c++_shared")
                cppFlags += listOf("-std=c++17")
            }
        }
    }

    externalNativeBuild {
        cmake {
            path = file("src/main/cpp/CMakeLists.txt")
            version = "3.22.1"
        }
    }

    buildTypes {
        release {
            isMinifyEnabled = false
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }
}

chaquopy {
    defaultConfig {
        version = "3.11"
        pip {
            // =================================================================
            // ADD YOUR PYTHON PACKAGE DEPENDENCIES HERE (one per line)
            // These are pure-Python or Android-prebuilt packages only.
            // Example: install("requests")
            // NOTE: packages that need native binaries (e.g. ollama, llama.cpp,
            // duckdb-based old chromadb builds) will likely NOT work on-device.
            // Pure sqlite3 (built into Python) works fine.
            // =================================================================
            install("flask")
        }
    }
}

dependencies {
    implementation("androidx.core:core-ktx:1.13.1")
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("androidx.activity:activity-ktx:1.9.0")
}
